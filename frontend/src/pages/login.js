import LoginForm from "../components/loginForm"

// Simply just use loginForm react component
const Login = () => {
  return (
    <div className="login">
      <LoginForm></LoginForm>
    </div>
  )
}

export default Login